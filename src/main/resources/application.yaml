spring:
  application:
    name: auth-app-backend
  profiles:
    active:
      - prod

server:
  port: 8082


#configuration
#datbase configuration
#mysql configuration

#email configuration
#oauth2 configuration